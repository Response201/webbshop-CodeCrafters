{
  "name": "webshop-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p ./frontend/scss/ && touch ./frontend/scss/style.css && sass ./frontend/scss/style.scss ./frontend/scss/style.css && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "axios": "^1.6.3"
  }
}
