{
  "name": "webshop-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build":  "mkdir -p ./public/scss/ && touch ./public/scss/style.css && sass ./src/scss/style.scss ./public/scss/style.css && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "axios": "^1.6.3"
  }
}
