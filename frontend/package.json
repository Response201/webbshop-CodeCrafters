{
  "name": "webshop-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  
  "scripts": {
    "dev": "vite",
    "build":  "mkdir -p ./src/scss/ && touch ./src/scss/style.css && sass ./src/scss/style.scss ./src/scss/style.css && tsc && vite build",
    "preview": "vite preview",
    "build": "vite build"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "axios": "^1.6.3"
  }
}
